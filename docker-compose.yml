services:
  app:
    build:
      dockerfile: Dockerfile
    image: password-strength-auditor:latest
    container_name: password-strength-auditor
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "
        echo '-----' &&
        echo 'Setup complete. Container is ready for development.' &&
        echo '-----' &&
        sleep infinity
      "
